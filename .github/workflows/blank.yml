# Starter workflow preserved from the original repository.
# Replaced by build.yml for this project.
name: Starter Workflow Placeholder

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "HELLO, MY NAME IS STEVEN uses build.yml for EPUB/static validation."
